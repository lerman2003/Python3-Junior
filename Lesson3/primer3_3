#Напишите программу, которая рассчитывает значение у, при заданном значении х:
#y= (sin x) ^2 при х>0 и 1-sin(x^2) в противном случае

from math import sin

x = float(input("Введите значение x: "))

if x>0:
    y=sin(x)**2    
else:
    y=1-sin(x**2)

print("Значение у = %.2f" %y)
